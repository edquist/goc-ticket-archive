#!/bin/bash

base=https://ticket.opensciencegrid.org/

#for x in {20000..20010}; do

#for x in {1..37870}; do
#  echo $x
#done | wget --base $base -i- -k -K -p -w .5 --random-wait -X '*this.thumbnail_url*'

#for x in {1..37870}; do
#  echo "attachment/list?id=$x"
#done | wget --base $base -i- -w .15 --random-wait -x

# note: attachements are listed in:
# https://ticket.opensciencegrid.org/attachment/list?id=31375

wget -i attchurls.list -w .3 --random-wait -x

